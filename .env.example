# Gateway configuration (all optional; none are secrets).
# HTTP port for the gateway.
PORT=8080
# Path for JSON state snapshots; unset means fully in-memory.
EXCHANGEKIT_SNAPSHOT=./snapshot.json
# Base URL used by demo.sh and the SDK integration tests.
EXCHANGEKIT_URL=http://localhost:8080
